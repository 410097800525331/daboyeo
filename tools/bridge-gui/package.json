{
  "name": "daboyeo-bridge-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Manual demo GUI for the DABOYEO Codex bridge worker.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "latest"
  }
}
